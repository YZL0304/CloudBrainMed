spring:
  profiles:
    include: route
  application:
    name: gateway-server
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
    sentinel:
      transport:
        dashboard: localhost:8858
      eager: true
  config:
    import: optional:nacos:gateway-server.yml
server:
  port: 80